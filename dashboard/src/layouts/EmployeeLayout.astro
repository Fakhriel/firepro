---
import Base from "./Base.astro";
import EmployeeSidebar from "../components/EmployeeSidebar.astro";
import EmployeeTopbar from "../components/EmployeeTopbar.astro";

export interface Props {
  title?: string;
  activeNav?: string;
}

const { title = "FIREPRO Teknisi", activeNav = "" } = Astro.props;

const API_URL = import.meta.env.PUBLIC_API_URL || "http://localhost:4000";
---

<Base title={`${title} — FIREPRO Teknisi`}>
  <div id="dashboard-root" data-api-url={API_URL} class="hidden min-h-screen flex bg-paper">

    <!-- ─── Sidebar (desktop) ─────────────────────────────── -->
    <EmployeeSidebar activeNav={activeNav} />

    <!-- ─── Main area ─────────────────────────────────────── -->
    <div class="flex-1 flex flex-col min-w-0 lg:ml-60">

      <!-- Mobile overlay (backdrop saat sidebar terbuka) -->
      <div
        id="sidebar-overlay"
        class="fixed inset-0 bg-ink/40 z-30 hidden lg:hidden"
      ></div>

      <EmployeeTopbar title={title} />

      <main class="flex-1 p-4 sm:p-6 lg:p-8 overflow-x-hidden">
        <slot />
      </main>

    </div>
  </div>

  <!-- Toast global -->
  <div id="toast" class="toast" role="alert" aria-live="polite">
    <span id="toast-msg"></span>
  </div>
</Base>

<script>
  import { getAdminAuth, clearAdminAuth } from "../lib/auth";

  // Guard client-side: hanya role 'karyawan' yang boleh melihat layout ini.
  // Role lain / belum login -> redirect ke /login. (Sebelumnya layout ini
  // TIDAK punya guard sama sekali — siapa pun bisa langsung akses halaman
  // /employee-technical/* tanpa login.)
  function guardKaryawanRole(): void {
    const { isAuthenticated, admin } = getAdminAuth();
    if (!isAuthenticated || !admin || admin.role !== "karyawan") {
      clearAdminAuth();
      window.location.replace("/login");
      return;
    }
    document.getElementById("dashboard-root")?.classList.remove("hidden");
  }
  guardKaryawanRole();

  // Mobile sidebar toggle
  function initSidebarToggle(): void {
    const toggle = document.getElementById("sidebar-toggle");
    const sidebar = document.getElementById("sidebar");
    const overlay = document.getElementById("sidebar-overlay");

    function open(): void {
      sidebar?.classList.remove("-translate-x-full");
      overlay?.classList.remove("hidden");
      document.body.style.overflow = "hidden";
    }
    function close(): void {
      sidebar?.classList.add("-translate-x-full");
      overlay?.classList.add("hidden");
      document.body.style.overflow = "";
    }

    toggle?.addEventListener("click", () => {
      const isOpen = !sidebar?.classList.contains("-translate-x-full");
      isOpen ? close() : open();
    });
    overlay?.addEventListener("click", close);
  }

  // Global toast helper
  function initToast(): void {
    window.showToast = (msg: string, type: "success" | "error" | "info" = "success") => {
      const toast = document.getElementById("toast");
      const toastMsg = document.getElementById("toast-msg");
      if (!toast || !toastMsg) return;

      toastMsg.textContent = msg;
      toast.classList.add("visible");

      if (type === "error") {
        toast.style.backgroundColor = "var(--color-signal)";
      } else {
        toast.style.backgroundColor = "var(--color-ink)";
      }

      clearTimeout((toast as HTMLElement & { _timer?: ReturnType<typeof setTimeout> })._timer);
      (toast as HTMLElement & { _timer?: ReturnType<typeof setTimeout> })._timer = setTimeout(() => {
        toast.classList.remove("visible");
      }, 3000);
    };
  }

  initSidebarToggle();
  initToast();
</script>
